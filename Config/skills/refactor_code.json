{
  "name": "refactor_code",
  "version": "1.0",
  "description": "Refactor and modernise existing Delphi code",
  "category": "refactoring",
  "agent_mode": "single-tool",
  "instructions": ["base", "delphi-expert"],
  "input_schema": {
    "type": "object",
    "properties": {
      "source_code": {
        "type": "string",
        "description": "Delphi source code to refactor"
      },
      "goal": {
        "type": "string",
        "description": "Refactoring goal (e.g. fix naming, remove duplication, fix N+1, modernise syntax)"
      }
    },
    "required": ["source_code"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "refactored_code": { "type": "string" },
      "changes": {
        "type": "array",
        "items": { "type": "string" },
        "description": "List of changes made"
      },
      "notes": { "type": "string" }
    }
  }
}
